VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Where the API is in development. Leave empty in production: the built app and
# /api share one origin there, and a bare path avoids CORS entirely.
VITE_API_URL=http://localhost:3001/api
